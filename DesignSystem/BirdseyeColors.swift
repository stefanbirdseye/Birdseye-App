import SwiftUI

extension Color {

    static let birdseyeNavy = Color(
        red: 0x00 / 255.0,
        green: 0x3F / 255.0,
        blue: 0x7D / 255.0
    )

    static let birdseyeAmber = Color(
        red: 0xF5 / 255.0,
        green: 0xA5 / 255.0,
        blue: 0x24 / 255.0
    )

    static let birdseyeSkyBlue = Color(
        red: 0x14 / 255.0,
        green: 0x3D / 255.0,
        blue: 0x5C / 255.0
    )

    static let birdseyeMutedTeal = Color(
        red: 0x32 / 255.0,
        green: 0x42 / 255.0,
        blue: 0x50 / 255.0
    )

    static let birdseyeBlueGray = Color(
        red: 0x57 / 255.0,
        green: 0x61 / 255.0,
        blue: 0x6D / 255.0
    )

    static let birdseyeWarmAmber = Color(
        red: 0xD8 / 255.0,
        green: 0x91 / 255.0,
        blue: 0x24 / 255.0
    )
    static let controlForeground = Color.primary
}
